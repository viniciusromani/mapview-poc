import SwiftUI

struct ProfileTabView: View {
    var body: some View {
        Text("Hello, World from ProfileTab!")
    }
}

struct ProfileTabView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileTabView()
    }
}
